#include <iostream>
#include "VisitFoldersUtil.h"
#include "ProcessFolders.h"
#include "PreFile.h"

using namespace std;
using namespace hfsfolder;
using namespace hfsfolder_util;
using namespace hfsfolder_model;

int main()
{
	//std::vector<path> lista = VisitFoldersUtil::getPathsFromFiles("C:\\ROMS-EMULS");
	
	//for (unsigned int i = 0; i < lista.size(); i++) {
		//path dir = lista.at(i);
		
		//PreFile prefile = ProcessFolders::attributesToPreFile(dir);
		
		//std::cout << dir.string() << "\n";
		//std::cout << prefile.toCVS() << "\n";
	//}
    
    return 0;
}
